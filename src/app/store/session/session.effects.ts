import { Injectable } from '@angular/core';

@Injectable()
export class SessionEffects {
    // Session effects can be added here for future API calls or side effects.
    // Currently no side effects needed as we favor in-memory state only.
}
